<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>com.apple.security.device.microphone</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Hardened runtime (obligatorio para notarizar con Developer ID).
         apple-events: notes.rs sincroniza con Apple Notas vía osascript; sin
         esto el runtime endurecido bloquea los Apple Events del proceso.
         disable-library-validation: la app carga dylibs de terceros
         (libonnxruntime, whisper) copiadas a Contents/Frameworks; el bundler
         las firma con la misma identidad, pero si alguna quedara sin firmar
         la app moriría al abrir (dyld) — mejor no depender de eso. -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
  </dict>
</plist>
