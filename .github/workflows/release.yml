name: "Release"

on: workflow_dispatch

jobs:
  create-release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    outputs:
      release-id: ${{ steps.create-release.outputs.result }}
      version: ${{ steps.get-version.outputs.version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Get version from tauri.conf.json
        id: get-version
        shell: bash
        run: |
          VERSION=$(grep -o '"version": "[^"]*"' src-tauri/tauri.conf.json | cut -d'"' -f4)
          echo "Application version from tauri.conf.json: $VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Create Draft Release
        id: create-release
        uses: actions/github-script@v9
        with:
          script: |
            const { data } = await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: `v${{ steps.get-version.outputs.version }}`,
              name: `v${{ steps.get-version.outputs.version }}`,
              draft: true,
              prerelease: false,
              generate_release_notes: true
            })
            return data.id

  publish-tauri:
    permissions:
      contents: write
    needs: create-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "macos-26" # for Arm based macs (M1 and above). Uses macOS 26 for Apple Intelligence SDK.
            args: "--target aarch64-apple-darwin"
            target: "aarch64-apple-darwin"
          - platform: "macos-latest" # for Intel based macs.
            args: "--target x86_64-apple-darwin"
            target: "x86_64-apple-darwin"
          - platform: "ubuntu-22.04" # Build .deb on 22.04
            args: "--bundles deb"
            target: "x86_64-unknown-linux-gnu"
          - platform: "ubuntu-24.04" # Build AppImage and RPM on 24.04
            args: "--bundles appimage,rpm"
            target: "x86_64-unknown-linux-gnu"
          - platform: "ubuntu-24.04-arm" # Build for ARM64 Linux
            args: "--bundles appimage,deb,rpm"
            target: "aarch64-unknown-linux-gnu"
          - platform: "windows-latest"
            args: ""
            target: "x86_64-pc-windows-msvc"
          - platform: "windows-11-arm" # for ARM64 Windows runner
            args: "--target aarch64-pc-windows-msvc"
            target: "aarch64-pc-windows-msvc"

    uses: ./.github/workflows/build.yml
    with:
      platform: ${{ matrix.platform }}
      target: ${{ matrix.target }}
      build-args: ${{ matrix.args }}
      # Firma macOS con la identidad que haya en el secret APPLE_CERTIFICATE:
      # Developer ID Application (hardened runtime + notarización con los
      # secrets APPLE_API_*) o, como respaldo, el autofirmado "Dilo Signing"
      # (sin notarizar; Gatekeeper lo cubre install.sh). Ver build.yml.
      sign-binaries: true
      asset-prefix: "dilo"
      upload-artifacts: false
      release-id: ${{ needs.create-release.outputs.release-id }}
      # Releases always build from scratch — never restore the Rust cache, so a
      # stale cached native lib can never be linked into a shipped artifact.
      no-cache: true
    secrets: inherit

  # "aarch64"/"x64" no le dicen nada a la mayoría de usuarios de Mac:
  # renombramos los assets de macOS a Mac-AppleSilicon / Mac-Intel.
  rename-mac-assets:
    permissions:
      contents: write
    needs: [create-release, publish-tauri]
    runs-on: ubuntu-latest
    steps:
      - name: Nombres amigables para los assets de macOS
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          rid="${{ needs.create-release.outputs.release-id }}"
          gh api "repos/${{ github.repository }}/releases/$rid/assets" --paginate \
            --jq '.[] | "\(.id) \(.name)"' | while read -r id name; do
            nuevo=$(echo "$name" | sed -E \
              -e 's/_aarch64\.dmg$/_Mac-AppleSilicon.dmg/' \
              -e 's/_x64\.dmg$/_Mac-Intel.dmg/' \
              -e 's/^Dilo_aarch64\.app\.tar\.gz$/Dilo_Mac-AppleSilicon.app.tar.gz/' \
              -e 's/^Dilo_x64\.app\.tar\.gz$/Dilo_Mac-Intel.app.tar.gz/')
            if [ "$nuevo" != "$name" ]; then
              gh api -X PATCH "repos/${{ github.repository }}/releases/assets/$id" \
                -f name="$nuevo" --jq '.name' | xargs -I{} echo "$name → {}"
            fi
          done
